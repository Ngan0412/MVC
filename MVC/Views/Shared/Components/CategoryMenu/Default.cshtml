@model IEnumerable<MVC.Models.Category>

@foreach (var item in Model)
{
    <li>
        <a class="dropdown-item" asp-controller="Shop" asp-action="Category" asp-route-id="@item.Id">
            @item.Name
        </a>
    </li>
}